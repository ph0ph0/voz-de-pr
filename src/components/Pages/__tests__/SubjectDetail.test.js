import { SubjectsMixed } from "../../../Constants/MockSubjectsData";

import { fetchSubject } from "../SubjectDetail";

global.log = () => {};

it("fetches subject", () => {
  expect(1).toEqual(1);
});
